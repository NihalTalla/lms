// Seeding script intentionally removed.
// The project must NOT include demo/mock users or problems. Seeding was removed to ensure
// environments are provisioned with real data only. If you previously relied on `prisma db seed`,
// running that command will no longer insert demo data.

console.info('prisma/seed.ts: seeding disabled — demo/mock data removed');

// Exit immediately with success. Keep file present so `prisma db seed` does nothing instead of failing.
process.exit(0);
